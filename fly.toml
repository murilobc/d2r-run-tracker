app = "d2r-run-tracker"
primary_region = "gru"

[build]
  dockerfile = "Dockerfile.frontend"

[env]
  API_URL = "https://d2r-run-tracker-api.fly.dev"

[http_service]
  internal_port = 8501
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0
